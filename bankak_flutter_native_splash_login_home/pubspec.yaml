name: bankak_native
version: 1.0.0+1
publish_to: 'none'
description: Native Flutter rebuild of splash, login, and home screens.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.9.2

flutter:
  uses-material-design: true
  assets:
    - assets/splash.mp4
    - assets/img/
