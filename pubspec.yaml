name: bok_vip_flutter
description: BOK VIP Flutter native conversion from PWA design.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.9.2

flutter:
  uses-material-design: true
  assets:
    - assets/splash.mp4
    - assets/img/
    - assets/icons/
